const weapons = ['candlestick', 'lead pipe', 'revolver']

const makeBroke = function(item) {
    return `broken ${weapons}`
}


const brokenWeapons = _.map(weapons, makeBroke)

brokenWeapons;



const abc1 = [];

